fastapi>=0.115
# Floor pinned explicitly: app/routers/cookies.py uses
# status.HTTP_422_UNPROCESSABLE_CONTENT, the RFC 9110 spelling starlette only
# grew in 0.48. An older resolve installs fine and then raises AttributeError
# at request time, not at import.
starlette>=0.48
uvicorn[standard]>=0.30
sqlalchemy>=2.0
aiosqlite>=0.20
aiofiles>=24.1
pydantic>=2.7
pydantic-settings>=2.3
yt-dlp>=2024.1.1
python-multipart>=0.0.9
# --- Archive ingest ---
httpx>=0.27
trafilatura>=2.1
readability-lxml>=0.8.1
nh3>=0.2.18
# Pinned explicitly: trafilatura already requires lxml, but if a resolver ever
# picks a version without a cp3xx wheel, pip silently falls back to the sdist and
# the Docker build dies in a Cython compile with no toolchain present.
lxml>=6.1.1
